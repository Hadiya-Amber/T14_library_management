{
  "name": "application",
  "version": "0.1.0",
  "private": true,
  "description": "Created by Propel provisioning for a repository that had no package manifest.",
  "scripts": {
    "test": "npx vitest run --environment jsdom"
  },
  "dependencies": {
    "react": "*",
    "typescript": "*",
    "vite": "*",
    "vitest": "*"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^7.0.1",
    "@testing-library/react": "^16.3.3",
    "@types/jsdom": "^30.0.0",
    "jsdom": "^30.0.1"
  },
  "vitest": {
    "test": {
      "environment": "jsdom"
    }
  }
}
