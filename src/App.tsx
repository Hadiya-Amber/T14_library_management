import React from 'react'
import LandingPage from './pages/LandingPage'

/**
 * App is the root component of the frontend application.
 * It mounts the LandingPage as the primary route for this scaffold.
 */
export default function App(): JSX.Element {
  return <LandingPage />
}
